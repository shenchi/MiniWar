using UnityEngine;
using System.Collections;

public class T_VIS : MonoBehaviour
{
    public TowerInfo selfInfo;
    // Use this for initialization
    void Start()
    {
        selfInfo = GetComponent<TowerInfo>();
    }

    // Update is called once per frame
    void Update()
    {

    }
}